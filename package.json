{
  "name": "ohm",
  "version": "0.0.0",
  "description":"Local message broker",
  "scripts": {
    "test": "crimp test/tests.json -g",
    "generateDocs":"doc-tool package_header src_interface > README.md",
    "fetch": "npx plusi install"
  },
  "devDependencies": {
    "dynamo": "git+ssh://git-codecommit.us-west-1.amazonaws.com/v1/repos/dynamo"
  }
}
