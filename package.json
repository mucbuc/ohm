{
  "name": "ohm",
  "version": "0.0.0",
  "description":"Local message broker",
  "scripts": {
    "test": "ccargs -p test/tests.json -t -c",
    "generateDocs":"doc-tool package_header src_interface test_example > README.md",
    "fetch": "npx plusi install",
    "format": "clang-format -i --style=Webkit src/*.h test/src/*.cpp"
 
  },
  "devDependencies": {
    "dynamo": "git+ssh://git-codecommit.us-west-1.amazonaws.com/v1/repos/dynamo"
  }
}
